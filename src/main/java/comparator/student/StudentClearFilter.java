package comparator.student;

import comparator.StudentComparator;
import models.Student;

public class StudentClearFilter implements StudentComparator {
    @Override
    public int compare(Student o1, Student o2) {
        return 0;
    }
}
